import { UserMode } from "@/types/viewmode";

export function AnnualBudgetSummary({
  isAuth,
  user,
}: {
  isAuth: boolean;
  user: UserMode;
}) {
  return (
    <>
      <div>家計簿モード</div>
    </>
  );
}
